'use strict';

/**
 * @ngdoc service
 * @name jetgrizzlyApp.videoQueue
 * @description
 * # videoQueue
 * Factory in the jetgrizzlyApp.
 */
angular.module('jetgrizzlyApp')
  .factory('videoQueue', ['config', '$window', '$firebase', function (config, $window, $firebase) {
    // services go here

    // public API here
    return {};
  }]);
